{
  "name": "{{ app.name }}-{{ job.name }}",
  "appId": "{{ app.name }}-{{ app.version}}",
  "archive": true,
  "inputs": {
        "job_inputs":  "agave://{{ app.deployment_system }}/{{ username }}/apps/{{ app.name }}-{{ app.version }}/{{ job.name }}"
          },
  "parameters": {"custom_modules": "python3"}
}
