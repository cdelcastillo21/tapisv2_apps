{
  "name": "{{ app.name }}-{{ job.name }}",
  "appId": "{{ app.name }}-{{ app.version}}",
  "archive": true,
  "inputs": {
        "job_assets":  "agave://{{ app.deployment_system }}/{{ username }}/apps/{{ app.name }}-{{ app.version }}/job_configs/{{ job.name }}"
          },
  "parameters": {"custom_modules": "tacc-singularity netcdf"}
}
