{
  "name": "{{ app.name }}-{{ job.name }}",
  "appId": "{{ app.name }}-{{ app.version}}",
  "archive": true,
  "inputs": {
        "generator_path":  "agave://{{ app.deployment_system }}/{{ username }}/apps/{{ app.name }}-{{ app.version }}/{{ job.name }}"
          },
  "parameters": {"custom_modules": "tacc-singularity netcdf"}
}
